#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#ifdef LOCAL
#include "debug.hpp"
#else
#define debug(...) (void)0
#endif

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  ${0}
  return 0;
}